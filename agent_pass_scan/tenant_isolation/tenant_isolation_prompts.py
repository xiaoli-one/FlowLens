DETECTOR_PROMPT = """租户隔离判断提示：
- 判断目标：tenant/org/company/corp/workspace/department/dept/team 等租户边界字段可控时，其他租户或未知租户身份是否能访问或操作当前租户的数据。
- 覆盖场景：多租户后台、SaaS 工作区、组织/公司维度数据、部门数据、租户配置、成员管理、项目/订单/账单、报表导出、邀请/审批、跨租户写操作。
- confirmed：swap_auth 主动验证返回 2xx/3xx/204/202，且响应仍包含原租户 ID/名称、原租户成员、项目、订单、配置、账单、导出数据，或写操作在其他身份下成功作用于原租户资源。
- likely：跨身份验证成功，响应结构与原租户基线高度一致，且 identity_memory 显示不同身份出现过不同租户值，但缺少明确账号和租户归属关系字段。
- needs_manual_review：只有 tenant/org/company 字段可控；没有其他身份认证；无法判断两个账号是否同租户；响应只显示通用结构或被截断。
- false_positive：401/403/404、跳登录、权限不足、只返回当前身份所属租户、空数据、公共组织/公开空间、两个账号属于同一租户、共享工作区、合作组织或授权成员关系。
- 不要把同租户成员互访、公开组织页、共享工作区当作租户隔离漏洞；必须证明租户边界被跨过。
- 对写方法，需要看到原租户资源被其他身份修改/删除/邀请/配置变更，或至少有明确成功标志和原租户标识。
- evidence 应包含：原租户基线、其他身份认证来源、跨身份验证响应里的租户标识或跨租户敏感内容；证据不足时写清缺少租户归属、其他租户身份或验证包。"""
