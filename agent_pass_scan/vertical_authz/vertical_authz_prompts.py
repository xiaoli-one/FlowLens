DETECTOR_PROMPT = """垂直越权判断提示：
- 判断目标：低权限、普通用户、访客升级后的普通身份，是否能访问或执行管理员、运营、财务、审批、审计、配置、用户/角色/权限管理等高权限功能。
- 覆盖场景：admin/manage/system/console/dashboard、用户列表、角色权限、RBAC/ACL、租户配置、审计日志、审批/审核、启停账号、导出敏感报表、财务/退款/发票、系统设置、运维接口。
- confirmed：有低权限身份证据，例如 identity_memory 中该身份只访问普通业务路径、响应角色字段不是管理员，或其他流量显示其普通用户属性；主动验证用该身份访问高权限接口返回 2xx/3xx/204/202，且响应包含管理数据、角色配置、用户列表、审计日志、系统配置、审批成功或高权限写操作成功。
- likely：目标路径和响应强烈体现高权限能力，且当前/替换身份不像管理员，但缺少明确角色字段；或多个非管理路径身份都能获得同类管理响应。
- needs_manual_review：只有 admin/manage/role 等路径关键词；没有低权限身份证据；响应只是前端菜单、空壳页面、通用配置；无法确认当前账号权限级别。
- false_positive：当前账号本身是管理员/运营/审核员；接口只是公共配置、静态资源、前端路由、菜单占位、帮助文档、权限不足提示、401/403/404、跳登录。
- 如果 swap_auth 返回另一个身份自己的普通页面、空数据或权限不足，不应 confirmed。
- 对写方法，需要看到高权限动作成功，例如角色变更、审批通过、禁用用户、配置保存、导出生成；仅 302 不足以确认，除非 Location/后续响应指向成功结果。
- evidence 应包含：低权限身份依据、目标高权限功能、主动验证包状态码、响应中体现高权限能力的字段或动作结果；证据不足时写清缺少低权限身份、角色字段或动作结果。"""
