DETECTOR_PROMPT = """水平越权/IDOR 判断提示：
- 判断目标：账号 B 使用自己的认证访问账号 A 的资源标识时，是否仍能读取、修改、删除或导出账号 A 的对象。
- 覆盖对象：用户/账号/成员、订单/账单/发票、文件/附件/下载、项目/工单/任务、地址/手机号/邮箱、购物车/优惠券、支付/退款、报表/导出、GraphQL object id、路径 ID、query/form/json 中的对象 ID。
- confirmed：swap_auth 主动验证返回 2xx/3xx/204/202，并且响应或后续流量仍对应账号 A 的同一对象，例如资源 ID、owner/user/account/customer、订单号、文件名、金额、手机号、邮箱、状态、创建人等与账号 A 基线一致；写/删/导出在账号 B 认证下成功作用于账号 A 对象也可 confirmed。
- likely：跨身份验证成功，响应结构和多个关键字段与账号 A 基线高度一致，但缺少明确 owner/账号字段；或 identity_memory 显示不同身份有不同资源值，而换认证后仍拿到原资源结构。
- needs_manual_review：只有 ID 可控、只有同接口多账号访问、没有成功跨身份验证；响应被截断；关键字段不足；无法判断资源归属或共享关系。
- false_positive：401/403/404、跳登录、权限不足、返回账号 B 自己的对象、空对象、脱敏占位、公开对象、分享链接、同团队/同租户内授权共享、资源本身无归属边界。
- 不能把“账号 A 和账号 B 都访问过同一接口”当漏洞；必须证明账号 B 访问的是账号 A 的具体资源，而不是同路径下自己的资源。
- 如果换认证后的响应字段和账号 A 基线不一致，尤其 owner/user/account 变成账号 B，应降为 false_positive 或 needs_manual_review。
- evidence 应包含：账号 A 基线包、账号 B 认证来源包、换认证访问账号 A 资源的验证包、响应中能锁定原对象归属未变的字段；证据不足时写清缺少 owner 字段、其他账号认证或主动验证响应。"""
