DETECTOR_PROMPT = """流程绕过判断提示：
- 判断目标：客户端能否直接提交或篡改 status/state/step/stage/approved/review/audit/workflow/published/paid/enabled/verified/deleted 等流程字段，从而跳过审批、支付、审核、发布、风控、前置步骤或顺序约束。
- 覆盖场景：审批通过、订单支付/退款状态、文章/商品发布、账号启停、实名认证/审核、工单流转、项目阶段、删除/恢复、库存/发货、优惠券/活动上线、任务完成。
- confirmed：mutate_param 主动验证把流程字段改为目标状态后返回 2xx/3xx/204/202，且响应确认状态变化、进入后续阶段、产生成功动作，或后续历史流量显示状态已生效。
- likely：变异请求成功且响应结构像保存/提交成功，但缺少明确最终状态字段；或历史流量显示未完成前置步骤却进入后续状态。
- needs_manual_review：只有状态字段可控；只有原始请求重放成功；缺少变异验证结果；无法判断正常流程规则；响应没有状态字段也没有成功动作。
- false_positive：字段只是搜索/筛选/排序条件、只读展示、前端缓存、服务端忽略篡改值、返回校验失败或权限不足、状态变化属于当前用户正常权限范围、当前接口本来就是合法状态流转接口。
- 不要把“客户端提交 status 字段”直接判漏洞；必须证明服务端接受了不应由客户端控制的状态，或绕过了应有前置步骤。
- 对写方法，204/202/302 可作为成功信号的一部分，但需要结合响应头、业务消息、后续状态或原对象字段确认。
- evidence 应包含：原始状态、篡改后的目标状态、主动验证包状态码、响应/后续流量中证明流程被推进或绕过的字段；证据不足时写清缺少正常流程规则、最终状态字段或后续状态确认。"""
